{
    "name": "learn-react",
    "version": "0.1.0",
    "private": true,
    "homepage": "https://jaydoncassimaty.github.io/learn-react",
    "dependencies": {
        "gh-pages": "^1.0.0",
        "mousetrap": "^1.6.1",
        "react": "^15.6.1",
        "react-dom": "^15.6.1",
        "react-scripts": "1.0.10"
    },
    "scripts": {
        "predeploy": "cross-env NODE_PATH=src npm run build",
        "deploy": "cross-env NODE_PATH=src gh-pages -d build",
        "start": "cross-env NODE_PATH=src react-scripts start",
        "build": "cross-env NODE_PATH=src react-scripts build",
        "test": "cross-env NODE_PATH=src react-scripts test --env=jsdom",
        "eject": "react-scripts eject"
    },
    "devDependencies": {
        "cross-env": "^5.0.1"
    }
}
